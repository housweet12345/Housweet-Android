<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.housweet.app">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:name="com.housweet.app.GlobalApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Housweet"
        android:usesCleartextTraffic="true"
        tools:targetApi="31"
        tools:replace="android:label">

<!--        <activity-->
<!--            android:name="com.housweet.presentation.MainActivity"-->
<!--            android:exported="true"-->
<!--            tools:replace="android:exported">-->
        <activity
            android:name="com.kakao.sdk.auth.AuthCodeHandlerActivity"
            android:exported="true"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <!-- Redirect URI: "kakao{nativeAppKey}://oauth" -->
                <data
                    android:scheme="${Kakao_Redirect_URI}"
                    android:host="oauth" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="com.kakao.sdk.AppKey"
            android:value="${Kakao_API_KEY}" />

        <meta-data
            android:name="com.naver.maps.map.CLIENT_ID"
            android:value="${Naver_Client_ID}" />
    </application>
</manifest>